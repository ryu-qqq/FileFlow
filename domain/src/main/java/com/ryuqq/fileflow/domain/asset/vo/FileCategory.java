package com.ryuqq.fileflow.domain.asset.vo;

/** 파일 카테고리. */
public enum FileCategory {

    /** 이미지 파일 (jpg, png, gif 등). */
    IMAGE,

    /** 비디오 파일 (mp4, mov 등). */
    VIDEO,

    /** 문서 파일 (pdf, docx 등). */
    DOCUMENT,

    /** 오디오 파일 (mp3, wav 등). */
    AUDIO,

    /** 기타 파일. */
    OTHER
}

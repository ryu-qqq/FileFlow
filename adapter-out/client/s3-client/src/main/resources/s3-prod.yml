# ============================================================
# S3 Client Layer 운영 환경 설정
# ============================================================
# 활성화: bootstrap의 application-prod.yml에서 import
# ============================================================

fileflow:
  s3:
    bucket: ${S3_BUCKET}
    region: ${AWS_REGION:ap-northeast-2}

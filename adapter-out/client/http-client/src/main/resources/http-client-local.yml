# ============================================================
# HTTP Client Layer 로컬 개발 환경 설정
# ============================================================
# 활성화: bootstrap의 application-local.yml에서 import
# ============================================================

fileflow:
  http-client:
    download:
      connect-timeout: 3000
      read-timeout: 30000
    callback:
      connect-timeout: 2000
      read-timeout: 5000
